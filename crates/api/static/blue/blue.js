(()=>{"use strict";const state={overview:null,bulletins:[],horizon:"hours_24"};const $=id=>document.getElementById(id);const esc=value=>String(value??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"})[c]);const date=value=>value?new Intl.DateTimeFormat("fr-FR",{dateStyle:"medium",timeStyle:"short",timeZone:"Europe/Paris"}).format(new Date(value)):"—";const score=value=>Number(value).toFixed(3).replace(".",",");const evaluation=value=>({pending:"En attente",researching:"Recherche en cours",signal_observed:"Signal observé",probable:"Probable",confirmed:"Confirmé",no_event_confirmed:"Aucun événement confirmé",inconclusive:"Non concluant"})[value]||value;async function json(url){const response=await fetch(url);if(!response.ok)throw new Error(`Service indisponible (${response.status})`);return response.json()}function showError(error){$("error").textContent=error.message;$("error").hidden=false}function renderHeader(){const {bulletin,interpretation}=state.overview;$("interpretation").textContent=interpretation;if(!bulletin)return;$("bulletin-date").textContent=new Intl.DateTimeFormat("fr-FR",{dateStyle:"long"}).format(new Date(`${bulletin.bulletin_date}T12:00:00Z`));$("bulletin-checksum").textContent=`Empreinte ${bulletin.checksum.slice(0,12)}…`;$("commune-count").textContent=Number(bulletin.commune_count).toLocaleString("fr-FR");$("coverage").textContent=`${Number(bulletin.mapped_cell_count).toLocaleString("fr-FR")} cellules couvertes`;$("count-24").textContent=Number(bulletin.alerts_24h).toLocaleString("fr-FR");$("count-48").textContent=Number(bulletin.alerts_48h).toLocaleString("fr-FR");$("model-version").textContent=`v1 · ${bulletin.model_version_id}`;$("issued-at").textContent=`Émis ${date(bulletin.issued_at)}`}function filtered(){const list=state.horizon==="hours_24"?state.overview.alerts_24h:state.overview.alerts_48h;const query=$("search").value.trim().toLocaleLowerCase("fr");const level=$("level").value;return list.filter(item=>(!level||item.risk_level===level)&&(!query||`${item.commune_name} ${item.insee_code} ${item.department_code||""}`.toLocaleLowerCase("fr").includes(query)))}function renderAlerts(){const rows=filtered();$("ledger-title").textContent=state.horizon==="hours_24"?"Échéance +24 h":"Échéance +48 h";$("visible-count").textContent=`${rows.length.toLocaleString("fr-FR")} communes`;$("empty").hidden=rows.length!==0;$("alerts").innerHTML=rows.map(item=>`<tr><td><strong>${esc(item.commune_name)}</strong><small>INSEE ${esc(item.insee_code)}${item.department_code?` · Dép. ${esc(item.department_code)}`:""}</small></td><td><span class="badge ${esc(item.risk_level)}"><span class="score">${score(item.alert_index)}</span></span></td><td class="score">${score(item.max_score)}</td><td>${Number(item.elevated_cell_count).toLocaleString("fr-FR")} / ${Number(item.evaluated_cell_count).toLocaleString("fr-FR")}</td><td>${date(item.valid_at)}</td><td><span class="badge">${esc(evaluation(item.evaluation_status))}</span></td></tr>`).join("")}function renderArchive(){$("bulletins").innerHTML=state.bulletins.map(item=>`<article class="archive-card"><strong>${esc(item.bulletin_date)}</strong><span>${Number(item.alerts_24h).toLocaleString("fr-FR")} vigilances à +24 h · ${Number(item.alerts_48h).toLocaleString("fr-FR")} à +48 h</span><span>Empreinte ${esc(item.checksum?.slice(0,12)||"—")}…</span></article>`).join("")}async function main(){$("clock").textContent=date(new Date().toISOString());document.querySelectorAll("[data-horizon]").forEach(button=>button.addEventListener("click",()=>{state.horizon=button.dataset.horizon;document.querySelectorAll("[data-horizon]").forEach(other=>other.classList.toggle("active",other===button));renderAlerts()}));$("search").addEventListener("input",renderAlerts);$("level").addEventListener("change",renderAlerts);try{[state.overview,state.bulletins]=await Promise.all([json("/api/blue/overview"),json("/api/blue/bulletins?limit=12")]);renderHeader();renderAlerts();renderArchive()}catch(error){showError(error)}}document.addEventListener("DOMContentLoaded",main)})();
